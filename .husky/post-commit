#!/bin/sh

if ! command -v claude >/dev/null 2>&1; then
  echo "claude CLI not found — skipping code review"
  exit 0
fi

echo ""
echo "Running /code-review… (Ctrl-C to skip)"
echo ""

claude -p "/code-review" --permission-mode auto --no-session-persistence
