// Entry point for the local agentic-intake agent.
//
// Placeholder so `agent/` is covered by typecheck + lint from the start
// (tkt-9c921bb56c1d). The real orchestration — local LLM tool loop, semantic
// retrieval over the board, and the human-in-the-loop approval gate — lands in
// the feature ticket. See AGENT-FLOW.md for the target architecture.

export const AGENT_PLACEHOLDER = true;
