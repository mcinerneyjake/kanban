// Entry point for the local agentic-intake agent.
//
// Still a placeholder: Phase 1 (semantic retrieval over the board) has landed
// in retrieval.ts, but the orchestration this entry point will own — the local
// LLM tool loop and the human-in-the-loop approval gate — lands in a later
// phase. See AGENT-FLOW.md for the target architecture.

export const AGENT_PLACEHOLDER = true;
